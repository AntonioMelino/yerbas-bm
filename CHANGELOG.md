# CHANGELOG.md — Yerbas BM

## [2026-07-28] - Fix: backend-railway-port-binding
- Rama: `feature/backend-railway-port`
- Qué se hizo: Con el Dockerfile ya funcionando, el contenedor arrancaba pero escuchaba en el puerto 80 por defecto en vez del puerto que Railway le asigna dinámicamente (variable `PORT`), porque la variable `ASPNETCORE_URLS=http://0.0.0.0:${{PORT}}` configurada a mano en Railway no se interpolaba y quedaba con el puerto vacío (`http://0.0.0.0:`, log: "Overriding HTTP_PORTS '8080'... Binding to values defined by URLS... Now listening on: http://[::]:80"). Se agregó en `Program.cs` una lectura directa de la variable de entorno `PORT` al arrancar (`builder.WebHost.UseUrls(...)` si está presente), que bindea Kestrel al puerto real inyectado por Railway en runtime, sin depender de que la interpolación de variables de Railway funcione. Verificado en local con `PORT=4321`: Kestrel loguea `Now listening on: http://0.0.0.0:4321`.
- Archivos principales afectados: `backend/YerbasBM.API/Program.cs`
- Autor: Claude

## [2026-07-28] - Fix: backend-dockerfile-railway
- Rama: `feature/backend-dockerfile`
- Qué se hizo: El primer intento de deploy en Railway falló porque Railpack (su builder) no reconoce `YerbasBM.slnx` (formato de solución `.slnx`, más nuevo que el `.sln` clásico) y no lograba detectar que el proyecto es .NET (`Railpack could not determine how to build the app`). Se agregó `backend/Dockerfile` (multi-stage: SDK 8.0 para restore/publish, runtime `aspnet:8.0` para la imagen final, apuntando explícitamente a `YerbasBM.API.csproj`) y `backend/.dockerignore`. Un `Dockerfile` en el Root Directory tiene prioridad sobre la auto-detección de Railway, así que evita el problema sin tocar `YerbasBM.slnx` ni la forma de compilar en desarrollo local.
- Archivos principales afectados: `backend/Dockerfile`, `backend/.dockerignore`
- Autor: Claude

## [2026-07-28] - Feature: backend-cors
- Rama: `feature/backend-cors`
- Qué se hizo: Se agregó configuración de CORS al backend, requisito bloqueante para el deploy (frontend en Vercel y backend en Railway quedan en dominios distintos, así que sin esto el navegador rechaza toda request cross-origin en producción). Los orígenes permitidos se leen de `Cors:AllowedOrigins` (array), configurable por entorno vía `Cors__AllowedOrigins__0`, `__1`, etc. En desarrollo, `appsettings.Development.json` ya trae `http://localhost:5173` (dev server de Vite, aunque hoy el proxy de Vite hace que no sea estrictamente necesario para ese flujo). En producción, sin ningún origen configurado la policy no habilita ninguno (fail-closed, en vez de abrir a cualquier origen) y se loguea un warning al arrancar, igual que ya se hacía con `Jwt:Key` ausente. Antes de deployar el frontend, hay que setear `Cors__AllowedOrigins__0` en Railway con la URL final de Vercel.
- Archivos principales afectados: `backend/YerbasBM.API/Program.cs`, `backend/YerbasBM.API/appsettings.json`, `backend/YerbasBM.API/appsettings.Development.json`, `backend/README.md`
- Autor: Claude

## [2026-07-28] - Feature: mobile-polish
- Rama: `feature/mobile-polish`
- Qué se hizo: Pulido visual y de usabilidad exclusivo para vista mobile. En `ProductCard.tsx` se redujeron paddings, tamaños de fuente, badges y botón de agregar para que la card sea más compacta y legible en pantallas chicas; la descripción se oculta en mobile. En `CatalogSection.tsx` se redujeron chips y búsqueda, se ajustó el título y el grid pasa a 2 columnas en mobile. En `HomePage.tsx` se redujeron paddings y tamaños de tipografía en Hero, Destacados, Nosotros y CTA; el grid de destacados también es de 2 columnas en mobile. En `Navbar.tsx` se achicaron logo/botones, se compactó el badge del carrito y el menú mobile ahora es pantalla completa con links grandes y tipografía display.
- Archivos principales afectados: `frontend/src/components/{ProductCard,CatalogSection,Navbar}.tsx`, `frontend/src/pages/HomePage.tsx`
- Autor: Kimi

## [2026-07-28] - Fix: home-categories-placement
- Rama: `feature/mobile-home-categories-fix`
- Qué se hizo: Se ocultó la sección de chips de categorías que aparecía arriba de "Productos destacados" tanto en mobile como en desktop. El catálogo completo (con sus propios filtros de categoría) sigue estando más abajo en la Home, justo antes del CTA de WhatsApp, que es donde el usuario puede explorar por categoría.
- Archivos principales afectados: `frontend/src/pages/HomePage.tsx`
- Autor: Kimi

## [2026-07-28] - Feature: home-catalog-section
- Rama: `feature/home-catalog-section`
- Qué se hizo: Se ajustó la Home para que respete fielmente la estructura del preview visual estático (`preview/index.html`): Hero → chips de categorías → Destacados → Nosotros → Catálogo completo → CTA WhatsApp. Se extrajo la sección de catálogo con filtros y búsqueda a un nuevo componente reutilizable `CatalogSection.tsx`, que se usa tanto en la Home (justo antes del CTA) como en `/productos` (manteniendo la sincronización con el query param `?categoria={slug}`). Los chips de categorías volvieron a su lugar original arriba de "Productos destacados", como ornamento de transición hacia los destacados.
- Archivos principales afectados: `frontend/src/pages/HomePage.tsx`, `frontend/src/pages/ProductsPage.tsx`, `frontend/src/components/CatalogSection.tsx`
- Autor: Kimi

## [2026-07-28] - Feature: admin-redesign
- Rama: `feature/admin-redesign`
- Qué se hizo: Se aplicó la identidad visual rústica oscura aprobada en el preview visual al panel de administración y su login, logrando coherencia estética con el sitio público. Se rediseñaron `LoginPage` (tarjeta centrada con logo BM, campos redondeados, botón lime/gold), `AdminLayout` (navbar oscuro con blur, navegación en chips y botón de salir estilizado), `ProductsAdminPage` (tabla con fondos forest/ink, badges lime/rojo y botones redondeados), `ProductFormPage` (formulario con etiquetas gold, inputs redondeados, caja de imagen destacada y vista previa) y `CategoriesAdminPage` (lista con bordes oliva, renombrado inline y botones alineados al diseño). Toda la funcionalidad previa se conserva: login JWT, CRUD de productos con multipart/form-data e imagen (validación cliente JPG/PNG/WebP, máx. 2 MB), CRUD de categorías con slug autogenerado por el backend, y redirección de rutas protegidas.
- Archivos principales afectados: `frontend/src/components/AdminLayout.tsx`, `frontend/src/pages/admin/{LoginPage,ProductsAdminPage,ProductFormPage,CategoriesAdminPage}.tsx`
- Autor: Kimi

## [2026-07-28] - Feature: redesign-public-ui-v2
- Rama: `feature/redesign-public-ui-v2`
- Qué se hizo: Se aplicó la dirección estética aprobada del preview visual estático (`preview/index.html`) a todo el sitio público. Se reemplazó la paleta anterior por la oficial del preview (`ink`, `night`, `forest`, `olive`, `yerba`, `gold`, `lime`, `cream`, `wa`) y las tipografías por `Cinzel Decorative` (display) + `Inter` (cuerpo). Se reescribieron `Navbar`, `Footer`, `ProductCard`, `ProductModal`, `CartDrawer`, `HomePage` y `ProductsPage` con la estética rústica oscura: hero con imagen de fondo y degradados, sección de categorías, destacados, “Nosotros”, catálogo con chips de filtro y búsqueda, drawer de carrito con pedido por WhatsApp y modal de producto. Se agregó `Toast.tsx` y se extendió `uiStore` con `toast`/`showToast` para confirmar acciones (por ejemplo, “Agregado al carrito 🧉”). Los assets visuales del preview se movieron a `public/assets/`. La carpeta temporal `preview/` se agregó a `.gitignore` para que no se commitee.
- Archivos principales afectados: `frontend/tailwind.config.js`, `frontend/src/index.css`, `frontend/.gitignore`, `frontend/src/components/{Navbar,Footer,ProductCard,ProductModal,CartDrawer,PublicLayout,Toast}.tsx`, `frontend/src/pages/{HomePage,ProductsPage}.tsx`, `frontend/src/stores/uiStore.ts`, `frontend/public/assets/*`
- Autor: Kimi

## [2026-07-27] - Feature: cart-whatsapp
- Rama: feature/cart-whatsapp
- Qué se hizo: Implementado el flujo completo carrito → pedido por WhatsApp (CONTEXTO.md secciones 7 y 8), todo del lado del frontend — no toca la API. Se agregó `uiStore` (Zustand, estado efímero de pantalla: producto seleccionado para el modal y visibilidad del drawer), `ProductModal` (detalle del producto con imagen grande, descripción, precio, selector de cantidad limitado al stock disponible y botón "Agregar al carrito"; si el producto no tiene stock el botón se deshabilita) y `CartDrawer` (panel que se desliza desde la derecha con fondo oscuro clickeable: lista de ítems con imágenes, botones +/- de cantidad, eliminar ítem, campo opcional para el nombre del cliente, total, botón verde estilo WhatsApp "Hacer pedido por WhatsApp" y "Vaciar carrito"). El mensaje del pedido se arma en `utils/whatsapp.ts` con el formato acordado (ítems con cantidad y subtotal, total, nombre opcional) y se abre `https://wa.me/{VITE_WHATSAPP_NUMBER}?text={mensaje}` en una pestaña nueva con `noopener,noreferrer`. Integración: `ProductCard` ahora es clickable y abre el modal, el ícono del carrito del Navbar pasó de elemento decorativo a botón que abre el drawer, y `PublicLayout` monta ambos componentes flotantes. Al agregar un producto desde el modal, éste se cierra y se abre el drawer para feedback inmediato. El carrito ya persistía en localStorage desde el setup inicial (Zustand `persist`). Se verificó `npm run build` (tsc + vite) y `npm run lint` en verde; la prueba visual del flujo completo queda para hacer con Antonio antes del merge (requiere productos cargados en la base).
- Archivos principales afectados: `frontend/src/stores/uiStore.ts`, `frontend/src/utils/whatsapp.ts`, `frontend/src/components/{CartDrawer,ProductModal,ProductCard,Navbar,PublicLayout}.tsx`, `frontend/README.md`
- Autor: Kimi

## [2026-07-27] - Feature: frontend-initial-setup
- Rama: feature/frontend-initial-setup
- Qué se hizo: Setup inicial completo del frontend (carpeta `frontend/`, antes vacía) con Vite + React 18 + TypeScript y estructura feature-based (CONTEXTO.md sección 4), conectado contra la API real desde el arranque — sin datos mock. Tailwind CSS con la paleta de colores y tipografías de la identidad visual (Cinzel Decorative + Inter, sección 7). Estado y datos: Zustand (`cartStore` con persistencia en localStorage — solo items/cantidades, el envío por WhatsApp es una feature posterior; y `authStore` para la sesión de admin) y TanStack Query para las llamadas a la API con invalidación automática tras las mutaciones. Cliente HTTP propio (`services/api.ts`, fetch sin axios) que centraliza URL base, header `Authorization: Bearer` y parseo de los tres formatos de error del backend (`{message}`, `{error}` y ProblemDetails). Parte pública: Navbar (con badge del carrito y menú mobile), HomePage (hero, destacados vía `isFeatured` y chips de categorías) y ProductsPage (filtro por categoría con chips sincronizados al query param `?categoria={slug}` + búsqueda por nombre en el cliente). Panel admin: `/admin/login` (POST /api/auth/login, JWT en localStorage, redirección si ya hay sesión), rutas protegidas por `AdminLayout` (redirige a login si no hay token), CRUD de productos (`/admin/productos`, `/admin/productos/nuevo`, `/admin/productos/:id/editar`) con formulario multipart/form-data real (input `type="file"`, campo `Image`; validación en cliente de tipo JPG/PNG/WebP y máx. 2 MB antes de enviar, espejando `ProductImageValidator` del backend, más vista previa de la imagen) y CRUD de categorías (`/admin/categorias`, alta + renombrado inline + eliminación; el slug nunca se envía, lo genera el backend). Como el backend no tiene CORS configurado, el dev server de Vite proxifica `/api` → `http://localhost:5035` (vite.config.ts), así en desarrollo el navegador nunca hace requests cross-origin. Se verificó: `npm run build` (tsc + vite) y `npm run lint` en verde, y contra el backend real corriendo en local: `GET /api/health`, `GET /api/products` y `GET /api/categories` (200 a través del proxy), `POST /api/categories` sin token (401) y login con credenciales inválidas (401 con el mensaje esperado). Nota: la rama `feature/frontend-initial-setup` había quedado creada desde un commit viejo de `develop` (sin el backend); se actualizó con fast-forward a `develop` antes de empezar.
- Archivos principales afectados: `frontend/package.json`, `frontend/vite.config.ts`, `frontend/tailwind.config.js`, `frontend/.env.example`, `frontend/index.html`, `frontend/src/{main,App}.tsx`, `frontend/src/types/index.ts`, `frontend/src/services/{api,products,categories,auth}.ts`, `frontend/src/stores/{cartStore,authStore}.ts`, `frontend/src/hooks/{useProducts,useCategories}.ts`, `frontend/src/components/{Navbar,Footer,ProductCard,Spinner,PublicLayout,AdminLayout}.tsx`, `frontend/src/pages/{HomePage,ProductsPage}.tsx`, `frontend/src/pages/admin/{LoginPage,ProductsAdminPage,ProductFormPage,CategoriesAdminPage}.tsx`, `frontend/README.md`
- Autor: Kimi

## [2026-07-27] - Feature: product-image-upload
- Rama: feature/product-image-upload
- Qué se hizo: Implementada la subida de imágenes de producto a Supabase Storage (bucket público `product-images`, sección 9 de CONTEXTO.md). Se agregó `IProductImageStorage` (Application) y su implementación `SupabaseProductImageStorage` (Infrastructure), que sube el archivo llamando directamente a la Storage REST API de Supabase vía `HttpClient` (sin sumar el SDK completo `supabase-csharp`, ya que solo hace falta subir un archivo). `POST /api/products` y `PUT /api/products/{id}` pasan a recibir `multipart/form-data`: la imagen (`Image`) es obligatoria al crear y opcional al actualizar (si no se envía, se conserva la imagen actual). Antes de subir, se valida tamaño (máx. 2 MB), Content-Type declarado (JPG/PNG/WebP) y los primeros bytes del archivo (magic bytes), para no confiar ciegamente en el Content-Type que manda el cliente. Cada imagen se sube con un nombre único (GUID) para evitar colisiones. Se probó el flujo completo contra el bucket real: creación con imagen (201, URL pública accesible), actualización sin imagen nueva (conserva la existente), y los tres rechazos esperados (archivo con contenido que no coincide con el Content-Type declarado, archivo que supera 2 MB, y sin token → 401 ya cubierto por `[Authorize]`).
- Bug encontrado corriendo contra Supabase real: `Supabase:Url` apuntaba a la URL del REST API (`.../rest/v1/`, tal como la muestra el dashboard de Supabase en otra sección) en vez de la URL base del proyecto, lo que rompía el path del Storage API y devolvía un 404 confuso (`PGRST125 Invalid path specified in request URL`, un código de error de PostgREST, pista de que la request ni siquiera llegaba al Storage API). Se corrigió el secret y además se agregó una normalización defensiva en `SupabaseProductImageStorage` que recorta un sufijo `/rest/v1` si alguien vuelve a configurar mal la URL.
- Archivos principales afectados: `backend/YerbasBM.Application/Interfaces/IProductImageStorage.cs`, `backend/YerbasBM.Application/Common/ProductImageValidator.cs`, `backend/YerbasBM.Infrastructure/Services/SupabaseProductImageStorage.cs`, `backend/YerbasBM.API/Requests/{CreateProductFormRequest,UpdateProductFormRequest}.cs`, `backend/YerbasBM.API/Controllers/ProductsController.cs`, `backend/YerbasBM.API/Program.cs`, `backend/YerbasBM.API/appsettings.json`, `backend/YerbasBM.Application/DTOs/{CreateProductDto,UpdateProductDto}.cs`
- Autor: Claude

## [2026-07-27] - Verificación: integración contra base real (Supabase)
- Rama: feature/verify-real-db-integration
- Qué se hizo: Todo el trabajo anterior (categories-crud, products-crud, admin-auth-jwt) se había verificado solo compilando o contra una base sin configurar; esta fue la primera prueba end-to-end contra la instancia real de Supabase. Se corrió `dotnet ef database update` contra la base real y se confirmó la creación de las tablas `admin_users`, `categories` y `products` (migration `InitialCreate`). Se levantó la API y se confirmó en los logs de arranque que `AdminUserSeeder` crea el usuario admin único al encontrar `admin_users` vacía y `Seed:AdminUsername`/`Seed:AdminPassword` configurados. Se probó el flujo completo: `GET /api/health` (200), `GET /api/categories` vacío (`[]`), `POST /api/auth/login` con las credenciales del seed (devuelve JWT válido), `POST /api/categories` sin token (401) y con token (201, categoría creada con slug autogenerado), y se limpió la categoría de prueba vía `DELETE /api/categories/{id}` (204) para dejar la base vacía. No se encontraron bugs ni comportamientos inesperados corriendo contra la base real: el fallback de `Jwt:Key` agregado en la feature anterior no se activó (confirma que los user-secrets cargan correctamente) y no hubo divergencias entre el esquema de EF Core y el de Supabase.
- Archivos principales afectados: ninguno (solo verificación; no hubo cambios de código)
- Autor: Claude

## [2026-07-26] - Feature: admin-auth-jwt
- Rama: feature/admin-auth-jwt
- Qué se hizo: Implementada la autenticación del panel admin con JWT (único rol autenticado del sistema, sin registro de clientes — sección 10 de CONTEXTO.md). Se agregaron los DTOs (`LoginDto`, `LoginResponseDto`), las interfaces `IAuthService`/`IAdminUserRepository`/`ITokenGenerator`, el repositorio EF Core (`AdminUserRepository`), el generador de tokens (`JwtTokenGenerator`, HMAC-SHA256 vía `System.IdentityModel.Tokens.Jwt`) y el servicio de negocio (`AuthService`, valida contraseña con BCrypt). Se expuso `AuthController` con `POST /api/auth/login`. Las mutaciones de admin existentes (`POST`/`PUT`/`DELETE` de `CategoriesController` y `ProductsController`) quedaron protegidas con `[Authorize]`; los `GET` siguen públicos. Se agregó `AdminUserSeeder`, que crea el único admin la primera vez que la API arranca con `admin_users` vacía y `Seed:AdminUsername`/`Seed:AdminPassword` configurados (no hay endpoint de registro). Durante las pruebas se detectó y corrigió un bug: con `Jwt:Key` sin configurar, el middleware de autenticación rompía con 500 **cualquier** request (incluido `/api/health`, que no depende de la base) — se resolvió generando una clave de firma aleatoria efímera como fallback cuando no hay `Jwt:Key`, sin habilitar por eso el login (que sigue requiriendo una key real vía `JwtTokenGenerator`). Se verificó que el proyecto compila sin errores y, en runtime sin base de datos configurada, que `/api/health` responde 200, `/api/categories` (GET) sigue público, y `POST /api/categories` sin token devuelve 401 antes de tocar la base.
- Archivos principales afectados: `backend/YerbasBM.Application/DTOs/{LoginDto,LoginResponseDto}.cs`, `backend/YerbasBM.Application/Interfaces/{IAuthService,IAdminUserRepository,ITokenGenerator}.cs`, `backend/YerbasBM.Application/Services/AuthService.cs`, `backend/YerbasBM.Infrastructure/Repositories/AdminUserRepository.cs`, `backend/YerbasBM.Infrastructure/Services/JwtTokenGenerator.cs`, `backend/YerbasBM.Infrastructure/Data/AdminUserSeeder.cs`, `backend/YerbasBM.API/Controllers/AuthController.cs`, `backend/YerbasBM.API/Controllers/{CategoriesController,ProductsController}.cs`, `backend/YerbasBM.API/Program.cs`, `backend/YerbasBM.API/appsettings.json`, `backend/README.md`
- Post-review: tras `security-reviewer`/`code-reviewer`, se corrigieron un bug (login sin manejar cuando falta `Jwt:Key`, ahora capturado y devuelve 500 seguro) y un hallazgo de seguridad HIGH (`Jwt:Key` sin mínimo de longitud, ahora exige 32 bytes/256 bits), y se agregó un log `Warning` al arrancar cuando `Jwt:Key` no está configurado. Quedan como pendientes no bloqueantes (documentados en sección 11 de CONTEXTO.md): rate limiting en el login y un timing side-channel que permite enumerar el username.
- Autor: Claude

## [2026-07-25] - Feature: products-crud
- Rama: feature/products-crud
- Qué se hizo: Implementado el CRUD completo de Productos siguiendo el mismo patrón Clean Architecture que Categories. Se agregaron los DTOs (`ProductDto`, `CreateProductDto`, `UpdateProductDto`), las interfaces `IProductRepository`/`IProductService`, el repositorio EF Core (`ProductRepository`, con `Include(Category)` y filtro opcional por slug de categoría) y el servicio de negocio (`ProductService`). Se expuso `ProductsController` con `GET /api/products` (acepta `?category={slug}`), `GET /api/products/{id}`, `POST /api/products`, `PUT /api/products/{id}` y `DELETE /api/products/{id}`, tal como define la sección 6 de CONTEXTO.md. `GET /api/products` solo devuelve productos con `isActive=true` (soft hide). La subida de imagen a Supabase Storage queda pendiente de una feature aparte; por ahora `imageUrl` se recibe como texto plano en el DTO. Se verificó que el proyecto compila sin errores; la prueba funcional contra datos reales queda pendiente de una conexión a PostgreSQL/Supabase configurada localmente (mismo estado que la feature de categorías).
- Archivos principales afectados: `backend/YerbasBM.Application/DTOs/{ProductDto,CreateProductDto,UpdateProductDto}.cs`, `backend/YerbasBM.Application/Interfaces/{IProductRepository,IProductService}.cs`, `backend/YerbasBM.Application/Services/ProductService.cs`, `backend/YerbasBM.Infrastructure/Repositories/ProductRepository.cs`, `backend/YerbasBM.API/Controllers/ProductsController.cs`, `backend/YerbasBM.API/Program.cs`
- Autor: Claude

## [2026-07-24] - Feature: categories-crud
- Rama: feature/categories-crud
- Qué se hizo: Implementado el CRUD completo de Categorías siguiendo Clean Architecture. Se agregaron los DTOs (`CategoryDto`, `CreateCategoryDto`, `UpdateCategoryDto`), las interfaces `ICategoryRepository`/`ICategoryService`, un generador de slugs (`SlugGenerator`, saca acentos y genera slugs únicos con sufijo numérico ante colisiones), el repositorio EF Core (`CategoryRepository`) y el servicio de negocio (`CategoryService`). Se expuso `CategoriesController` con `GET /api/categories`, `POST /api/categories`, `PUT /api/categories/{id}` y `DELETE /api/categories/{id}`, tal como define la sección 6 de CONTEXTO.md. El slug se genera automáticamente a partir del nombre (no se recibe del cliente). Se verificó que el proyecto compila sin errores y que las 4 rutas quedan correctamente registradas (confirmado vía Swagger); la prueba funcional contra datos reales queda pendiente de una conexión a PostgreSQL/Supabase configurada localmente.
- Archivos principales afectados: `backend/YerbasBM.Application/DTOs/{CategoryDto,CreateCategoryDto,UpdateCategoryDto}.cs`, `backend/YerbasBM.Application/Common/SlugGenerator.cs`, `backend/YerbasBM.Application/Interfaces/{ICategoryRepository,ICategoryService}.cs`, `backend/YerbasBM.Application/Services/CategoryService.cs`, `backend/YerbasBM.Infrastructure/Repositories/CategoryRepository.cs`, `backend/YerbasBM.API/Controllers/CategoriesController.cs`, `backend/YerbasBM.API/Program.cs`
- Autor: Claude

## [2026-07-22] - Feature: backend-initial-setup
- Rama: feature/backend-initial-setup
- Qué se hizo: Setup inicial del backend .NET 8 con Clean Architecture (proyectos API, Application, Domain, Infrastructure). Se configuró Entity Framework Core con Npgsql (PostgreSQL/Supabase), se crearon las entidades `Category`, `Product` y `AdminUser` según el modelo de datos de la sección 5 de CONTEXTO.md, y se generó la migration inicial (`InitialCreate`) que crea las tablas `categories`, `products` y `admin_users`. Se agregó un endpoint de salud (`GET /api/health`) y se verificó que el proyecto levanta y responde localmente.
- Archivos principales afectados: `backend/YerbasBM.sln`, `backend/YerbasBM.API/Program.cs`, `backend/YerbasBM.API/Controllers/HealthController.cs`, `backend/YerbasBM.Domain/Entities/*.cs`, `backend/YerbasBM.Infrastructure/Data/YerbasBMDbContext.cs`, `backend/YerbasBM.Infrastructure/Data/Migrations/*_InitialCreate.cs`, `backend/README.md`, `backend/.gitignore`
- Autor: Claude
